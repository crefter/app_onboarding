name: app_onboarding
description: "Help you create onboarding with tooltips"
version: 1.0.1
homepage: https://github.com/crefter/app_onboarding
repository: https://github.com/crefter/app_onboarding
issue_tracker: https://github.com/crefter/app_onboarding/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter: